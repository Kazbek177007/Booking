#include <QtCore>

POD Product(ProductPreview preview, bool isPublished)

class ListOfProducts
{
    PROP(QList<Product> products);
    SLOT(void addProduct());
    SLOT(void changeProductInfo(Product));
    SLOT(void removeProduct(int id));
};

//POD Order(QMap<int, int> items, QDateTime dateTime)

class ListOfOrders
{
  PROP(QList<Order> orders);
};